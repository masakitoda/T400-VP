import React from "react";

const Result = ({ styles }) => (
    <p className={`py-4 px-6 font-poppins font-medium text-[18px] text-primary1 bg-blue-gradient outline-none ${styles}`}>登録ありがとうございます。</p>
);

export default Result;